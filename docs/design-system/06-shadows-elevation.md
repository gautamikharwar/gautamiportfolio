# Shadows & Elevation

No drop shadows currently — the design uses 1px borders for separation
instead. If elevation is needed later:

| Token       | Value                        | Usage                           |
| ----------- | ---------------------------- | ------------------------------- |
| `shadow-sm` | `0 1px 2px rgba(0,0,0,0.05)` | Hover state on cards (optional) |
